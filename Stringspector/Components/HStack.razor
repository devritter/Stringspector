@{
    var cssClass = $"hstack default-gap {CssClass}";
}

<div class="@cssClass" style="display: flex; flex-direction: column;">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }

    [Parameter] public string? CssClass { get; set; }
}